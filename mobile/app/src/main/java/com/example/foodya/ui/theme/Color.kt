package com.example.foodya.ui.theme

import androidx.compose.ui.graphics.Color

// --- MÀU CHỦ ĐẠO (Brand Colors) ---
val PrimaryOrange = Color(0xFFFF6D00) // Màu cam đậm (Dùng cho nút chính, icon quan trọng)
val PrimaryLight = Color(0xFFFF9E40)  // Màu cam nhạt (Dùng cho background nhẹ)
val SecondaryYellow = Color(0xFFFFC107) // Màu vàng (Dùng cho sao đánh giá, khuyến mãi)

// --- MÀU NỀN & CHỮ ---
val White = Color(0xFFFFFFFF)
val Black = Color(0xFF121212)
val LightGray = Color(0xFFF5F5F5) // Màu nền tổng thể (Background)
val DarkGray = Color(0xFF757575)  // Màu chữ phụ

// --- MÀU TRẠNG THÁI ---
val ErrorRed = Color(0xFFD32F2F)  // Màu báo lỗi
val SuccessGreen = Color(0xFF388E3C) // Màu báo thành công